networks:
  msa-net:
    external: true

services:
  user:
    image: dlsqja3436/board
    container_name: board
    env_file:
      - ./.env
    restart: unless-stopped
    networks:
      - msa-net
