# 1. AI 훈련 데이터 및 결과 (필수 제외)
# ---------------------------------------------------------------------
# 원본 이미지, 라벨, 분할된 데이터가 있는 폴더
yolo_data/
# YOLO 훈련 로그, 그래프, 그리고 최종 모델 가중치(best.pt)가 포함된 폴더
runs/

# ---------------------------------------------------------------------
# 2. 모델 가중치 및 환경 파일
# ---------------------------------------------------------------------
# 다운로드한 사전 훈련된 모델 및 모든 모델 파일 (*.pt는 runs/에 포함되지만 명시)
*.pt
ai_analysis/yolo_model/weights/

*.pth
*.h5
# Python 가상 환경 폴더
.venv/
.conda/
# 가상 환경 폴더명 예시
yolov8_env/
jellypaw_cv/

# ---------------------------------------------------------------------
# 3. Python 및 OS 캐시
# ---------------------------------------------------------------------
__pycache__/
*.pyc
.DS_Store

# 4. 테스트/임시 파일 추가
# ---------------------------------------------------------------------
# 임시 이미지 파일 (분석에 필수적인 샘플 이미지가 아닌)
*.png
*.jpg
# 테스트 스크립트 파일(for 분석 로직과 분리)
test_*.py